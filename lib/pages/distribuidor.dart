import 'package:flutter/material.dart';

class DistribuidorPage extends StatelessWidget {
  const DistribuidorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Distribuidor'));
  }
}
