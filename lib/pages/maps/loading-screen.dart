import 'package:flutter/material.dart';

class LoagingScreen extends StatelessWidget {
   
  const LoagingScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('LoagingScreen'),
      ),
    );
  }
}